package com.example.Adult_Shop.service;

import com.example.Adult_Shop.product.Product;
import lombok.NoArgsConstructor;

@NoArgsConstructor
public class InventoryService {

    //private Inventory inventory;

    public boolean checkInventory() {
    //    if (inventory == null || inventory.getProducts() == null) {
    //         return false;
    //    }
    //    return inventory.getProducts().contains(product);
        return true;
    }
}
